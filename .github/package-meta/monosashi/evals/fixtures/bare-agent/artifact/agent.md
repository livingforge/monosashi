---
name: deploy-agent
description: An autonomous deployment agent that runs a rollout end to end.
---

# deploy-agent

An agent that drives a rollout. It runs each stage in order until done. See
`scripts/controller.mjs`.
